import { IDashboardHostState, IDashboardState } from './app.model';

const dashboardState: IDashboardState = {
  id: '',
  mode: 'view',
};

/* Dashboard State */
export const initialState: IDashboardHostState = {
  dashboardState,
};
